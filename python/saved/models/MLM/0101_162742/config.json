{
    "name": "MLM",
    "logging_verbosity": 1,
    "logging_to_file": true,
    "logging_dir": "log",
    "n_gpu": 0,
    "seed": 10,
    "model": {
        "type": "MLMBertModel",
        "args": {}
    },
    "dataset": {
        "type": "ReviewsDataset",
        "args": {
            "n_samples": 100
        }
    },
    "dataloader": {
        "type": "DataLoader",
        "args": {
            "batch_size": 4,
            "shuffle": true,
            "num_workers": 4
        }
    },
    "optimizer": {
        "type": "SGD",
        "args": {
            "lr": 0.001,
            "weight_decay": 0
        }
    },
    "loss": "cross_entropy_loss",
    "metrics": [
        "accuracy"
    ],
    "lr_scheduler": {
        "type": "StepLR",
        "args": {
            "step_size": 50,
            "gamma": 0.1
        }
    },
    "trainer": {
        "epochs": 100,
        "save_dir": "saved/",
        "save_period": 1,
        "verbosity": 1,
        "monitor": "min val_loss",
        "early_stop": 10,
        "tensorboard": true
    }
}